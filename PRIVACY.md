# 隐私说明（Privacy Policy）

本扩展（DSH 提问草稿保全）不收集、不传输任何个人数据。

- 数据种类：你在 DSH 提问卡片里填写的选项与文字（草稿）。
- 存储位置：本机浏览器的 `chrome.storage.local`，键名以 `qd:` 开头，按标签页隔离。
- 传输：不发生任何网络请求；扩展不含远程代码、不含分析/统计/广告。
- 保留期：草稿超过 24 小时自动删除；点「提交 / 跳过本题」或把填入内容发送后立即删除。
- 权限用途：`storage` 仅用于保存上述草稿；站点权限仅覆盖本机 DSH 页面（`127.0.0.1`、`localhost`）与你自建的 DSH 域名。
- 删除方式：`chrome://extensions` → 移除扩展，或在 DevTools 执行 `chrome.storage.local.clear()`。

联系方式：GitHub Issues（https://github.com/brestain/dsh-question-draft/issues）
